apiVersion: v1
data:
  {{- $files := .Files }}
  {{- range tuple "create-database.sh" }}
  {{ . }}: |-
        {{ $files.Get . | nindent 8 }}
  {{- end }}
kind: ConfigMap
metadata:
  name: create-dbs
