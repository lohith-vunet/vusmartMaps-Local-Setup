apiVersion: v1
kind: Service
metadata:
  name: alert
  labels:
    app: alert
    managedBy: vunet
    {{- include "cairo.labels" . | nindent 4 }}
spec:
  ports:
    - port: 50052
      name: alert
  selector:
    app: alert
    managedBy: vunet
