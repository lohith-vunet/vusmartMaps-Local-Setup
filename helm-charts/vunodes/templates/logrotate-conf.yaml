---
apiVersion: v1
data:
  {{- $files := .Files }}
  {{- range tuple "logrotate.conf" }}
  {{ . }}: |-
        {{ $files.Get . | nindent 8 }}
  {{- end }}
kind: ConfigMap
metadata:
  creationTimestamp: null
  name: logrotate-config
