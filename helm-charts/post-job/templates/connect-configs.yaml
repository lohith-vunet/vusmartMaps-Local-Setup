---
apiVersion: v1
data:
  {{- $files := .Files }}
  {{- range tuple  "enrichment.json"  "kafka_setup.sh" }}
  {{ . }}: |-
        {{ $files.Get . | nindent 8 }}
  {{- end }}
kind: ConfigMap
metadata:
  creationTimestamp: null
  name: connector-configs
