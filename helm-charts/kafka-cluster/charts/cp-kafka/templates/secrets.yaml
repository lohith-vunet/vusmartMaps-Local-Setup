apiVersion: v1
kind: Secret 
metadata:
  name: kafka-ssl-store
data:
  keystore.jks:  {{ .Files.Get "secrets/keystore.jks.b64" | quote }}
  truststore.jks:  {{ .Files.Get "secrets/truststore.jks.b64" | quote  }}
  cert_creds:  {{ .Files.Get "secrets/cert_creds.b64" | quote }}
