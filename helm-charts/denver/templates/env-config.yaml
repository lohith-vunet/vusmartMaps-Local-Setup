apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "denver.fullname" . }}-env-config
  labels:
  {{- include "denver.labels" . | nindent 4 }}
data:
  {{ .Values.envConfig | toYaml | nindent 2 }}
